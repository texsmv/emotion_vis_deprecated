import 'package:flutter/material.dart';

class VisualizationsView extends StatelessWidget {
  const VisualizationsView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
